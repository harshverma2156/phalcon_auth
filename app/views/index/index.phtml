<?php
use Phalcon\Mvc\Application;

require '../vendor/autoload.php';

$di = require '../app/config/services.php';

$app = new Application($di);

try {
    echo $app->handle($_SERVER['REQUEST_URI'])->getContent();
} catch (Exception $e) {
    echo 'Exception: ', $e->getMessage();
}